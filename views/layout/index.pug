doctype html
html
    head
        title Express Blog
        link(href="/static/styles/style.css" rel="stylesheet")
    body
        header
            .header
                a(href="/") Главная
                a(href="/login").ml-2 Вход
                a(href="/registration").ml-2 Авторизация
                if (!currentUser.isGuest())
                    form(action="/session?_method=DELETE" method="POST").ml-2
                        button(type="submit") Выход
        hr
        block content
